Per installare i pacchetti prima cloni la repository dentro a xamp/htdocs, poi:
----
cd frontend
npm install
----
per startare il server e quindi visualizzare tutto (tipo che quando salvi ti si aggiorna la pagina):
npm run dev
(per aprire nel browser metti localhost:5173)
